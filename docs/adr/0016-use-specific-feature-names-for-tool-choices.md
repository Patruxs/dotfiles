# Use Specific Feature Names For Tool Choices

When a feature represents a choice between likely alternatives, the feature name includes the chosen tool. For example, use `webserver_nginx` rather than a generic `webserver`, leaving room for future names like `webserver_caddy` without adding feature options.
