# Desktop Setup Is Explicit Features

Desktop setup uses both grouped features and explicit special-purpose features. Profiles can select broad desktop capabilities like `desktop_base` or `desktop_apps`, while heavier or riskier tools such as terminals, IDE helpers, virtualization, and Docker Desktop remain explicit so server profiles do not inherit them accidentally.
