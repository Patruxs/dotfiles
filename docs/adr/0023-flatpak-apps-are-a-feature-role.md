# Flatpak Apps Are A Feature Role

Flatpak desktop apps are installed through a `flatpak_apps` feature role rather than plain package-set entries. The role owns procedural setup such as configuring Flathub before installing the selected apps.
