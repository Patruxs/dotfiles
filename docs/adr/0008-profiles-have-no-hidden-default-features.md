# Profiles Have No Hidden Default Features

Profiles receive only the features they list explicitly. Even common capabilities such as `core_cli` must appear in each profile that needs them, making every profile readable as the complete intended setup for that machine purpose.
