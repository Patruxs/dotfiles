# Bootstrap And Ansible Validate Different Things

Bootstrap validates that the detected operating system maps to a known platform playbook before Ansible starts. Ansible preflight validates profile existence, supported platforms, selected features, and feature implementation support for the chosen platform.
