# Phase 1 Adds Feature Lists With Compatibility Vars

The first migration phase introduces explicit feature lists in profile files while preserving current behavior. Existing role variables such as AI CLI toggles or Linux native app lists may be derived from selected features temporarily until roles are converted to the new model.
