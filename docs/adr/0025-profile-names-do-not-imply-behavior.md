# Profile Names Do Not Imply Behavior

Profile names such as `personal`, `work`, or `server` are human-facing labels and do not imply setup behavior by themselves. The selected feature list is the contract; if a work machine needs desktop setup, the work profile lists desktop features explicitly.
