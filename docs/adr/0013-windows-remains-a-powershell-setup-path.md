# Windows Remains a PowerShell Setup Path

Windows remains on its existing PowerShell and winget setup path for now. The Unix/macOS refactor can use one Ansible playbook per platform without forcing Windows into that structure, while still allowing Windows to adopt the shared profile and feature language later.
