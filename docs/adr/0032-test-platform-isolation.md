# Test Platform Isolation

Implementation should include regression tests proving that a selected platform loads only its own playbook and package set. Tests should also cover early failure for unknown features and unsupported profile/platform combinations before installation starts.
