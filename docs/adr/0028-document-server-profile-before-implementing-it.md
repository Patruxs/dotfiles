# Document Server Profile Before Implementing It

The server profile is documented as a planned example before it becomes a runnable Ansible profile. This shows the target design pressure without implying server setup is supported before platform and feature validation exist.
