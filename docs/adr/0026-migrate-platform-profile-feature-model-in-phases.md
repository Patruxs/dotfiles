# Migrate Platform/Profile/Feature Model In Phases

The platform, profile, and feature refactor will be migrated in phases rather than as one large rewrite. The migration should preserve existing personal and work behavior first, then split platform playbooks and package sets, then convert special installers into feature roles, and only add the server profile once the model is stable.
