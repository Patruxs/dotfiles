# Features May Be Data Only, Role Only, Or Both

Selected features are valid when they have a package-set entry, a matching feature role, or both. Execution installs package-set entries and runs only the selected features that have role directories, avoiding empty roles for simple package groups.
