# Package Sets Group Entries By Installer Type

Package sets group package entries by installer type, such as `apt`, `dnf`, `pacman`, `brew`, `cask`, `winget`, or `flatpak`. This lets each feature map to the installers needed on a platform while keeping package sets as declarative data.
