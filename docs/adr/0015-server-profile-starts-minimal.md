# Server Profile Starts Minimal

The server profile starts with minimal automation: install selected server tools and enable obvious services. Security hardening, firewall policy, TLS certificates, reverse proxy configuration, and deeper production behavior should be added later as separate explicit features.
