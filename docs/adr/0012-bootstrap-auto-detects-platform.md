# Bootstrap Auto Detects Platform

Bootstrap detects the current platform by default and chooses the matching platform playbook. A platform override may exist for CI or tests, but real machine setup must not use an override that disagrees with the detected operating system.
