# Per-Platform Package Sets Become The Package Source

The existing global package data is replaced gradually by per-platform package sets. During migration, old package data may remain as compatibility, but the target state has one authoritative package source per platform to avoid drift.
