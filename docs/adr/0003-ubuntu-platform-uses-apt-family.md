# Ubuntu Platform Uses Apt Family

Ubuntu has its own platform playbook and is the only supported Debian-family operating system for now. The Ubuntu platform can name `apt` as its package family so future Debian support can reuse package-manager ideas without making Debian support implicit today.
