# Platform Playbooks Own Execution Order

Profiles select features but do not define execution order. Each platform playbook owns the safe sequence for validation, package installation, feature roles, service configuration, and Chezmoi application so profile files remain declarative.
