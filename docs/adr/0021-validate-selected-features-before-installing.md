# Validate Selected Features Before Installing

Setup performs preflight validation before any package or app installation starts. If a selected feature is unknown, unsupported by the current platform, or missing an implementation, setup fails early instead of leaving the machine partially installed.
