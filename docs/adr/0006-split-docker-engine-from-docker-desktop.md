# Split Docker Engine From Docker Desktop

Docker Engine and Docker Desktop are separate features. They install different things and can conflict on Linux, so profiles must choose the intended Docker capability explicitly instead of using a generic `docker` feature.
