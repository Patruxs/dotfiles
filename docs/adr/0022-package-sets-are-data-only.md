# Package Sets Are Data Only

Package sets contain only package or app identifiers that the platform package manager can install directly. Any setup requiring repositories, remotes, keys, scripts, downloaded archives, services, or conditionals belongs in a feature role.
