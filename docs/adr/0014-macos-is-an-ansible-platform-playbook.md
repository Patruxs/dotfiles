# macOS Is an Ansible Platform Playbook

macOS remains part of the Ansible setup model as its own platform playbook. It uses Homebrew internally, but follows the same profile and feature structure as Ubuntu, Fedora, and Arch.
