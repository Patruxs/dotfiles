---
- name: Install JetBrains Toolbox feature on Linux
  include_tasks: "{{ chezmoi_dir }}/ansible/roles/packages/tasks/linux-jetbrains-toolbox.yml"
  when:
    - dotfiles_platform.system == 'Linux'
    - not (dotfiles_ci | default(false))
