---
- name: Run chezmoi apply
  command:
    argv:
      - chezmoi
      - apply
      - --source
      - "{{ chezmoi_dir }}"
      - --force
      - -v
  environment:
    PROFILE: "{{ profile }}"
  register: chezmoi_result
  changed_when: false
