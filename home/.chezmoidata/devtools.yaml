devtools:
  # Global npm tools, installed or updated to @latest by the devtools role
  # (Linux, macOS) and bootstrap.ps1 (Windows). Language runtimes come from
  # the platform package sets, always at their newest available version.
  npm_global_packages:
    - playwright
